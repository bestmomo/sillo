<?php
include_once 'frameworks.php';
?>

<div>
    <x-header title="Frameworks" shadow separator progress-indicator>
    </x-header>

    {{-- <div class="mt-3">

        <ul>

            @foreach ($frameworksLinks as $framework => $links)
                <li>{{ ucfirst($framework) }}</li>
                <ul>

                    @foreach ($links as $link)
                        <li>- {{ Str::ucfirst($link) }}</li>
                    @endforeach

                </ul>
            @endforeach

        </ul> --}}

        {{-- <livewire:gc7.abc.livewire.hello-world /> --}}
        {{-- <livewire:gc7.abc.livewire.counter /> --}}
        {{-- <livewire:gc7.abc.livewire.counter by=3/> --}}

        {{-- <livewire:gc7.abc.livewire.Todos /> --}}

        {{-- {{ dd($links) }} --}}
    </div>
</div>
